services:
  - type: web
    runtime: node
    name: signal-bot-router
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
